# Secrets — never commit
.env

# Node
node_modules/
dist/
*.log

# Build artifact (regenerated by npm run dev)
client/static/bundle.js

# Local logs / trajectory data
data/
*.jsonl

# Emitted by npm run bundle:handout — the fixture SOURCE is tracked, the
# build output is not.
fixtures/*.handout.json
